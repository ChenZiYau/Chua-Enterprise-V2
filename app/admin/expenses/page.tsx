import { PagePlaceholder } from "@/components/admin/PagePlaceholder";

export default function ExpensesPage() {
  return (
    <PagePlaceholder
      title="Expenses"
      description="A consolidated expenses ledger will live here. For now, use the Properties page and click ‘Add Expense’ on any property to record costs."
    />
  );
}
